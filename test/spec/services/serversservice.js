'use strict';

describe('Service: serversService', function () {

  // load the service's module
  beforeEach(module('mycsServersApp'));

  // instantiate service
  var serversService;
  beforeEach(inject(function (_serversService_) {
    serversService = _serversService_;
  }));

  // it('should do something', function () {
  //   expect(!!serversService).toBe(true);
  // });

});
